# Upgrade Plan — 0.16.0

> Auto-generated from `docs/INFRA-CHANGELOG.md` — review before applying.

## 1. Critical Changes (must apply before upgrade)

*(None)*

---

## 2. High-Priority Changes

### 2.1 Deprecations & Removals

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `app.nim-service-expose-ingress-config.enabled` and `app.nim-service-expose-ingress-config.annotations` removed | **Remove** | Remove custom enable flag and annotation overrides from application config or Helm values. Use `K8S_NIM_INGRESS_CLASS_NAME` if only the legacy NIM ingress class must be customized. |

---

## 3. Medium-Priority Changes (defaults changed — review)

### 3.1 Deployment & Scaling

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `RESOURCES_STORAGE_MAX_SIZE` added | **Review** | Sets the maximum allowed storage size for NIM deployments. Default is `200Gi`. Review if your deployments require larger storage. |
| 2 | Progress deadline fallback | **Review** | When no startup probe is configured, KServe and NIM deployments now use `startup-timeout + buffer-seconds` as the progress deadline fallback. Review if you have deployments without startup probes that might be affected by this timeout calculation. |
| 3 | `K8S_NIM_CLUSTER_HOST` behavior changed | **Review** | Keep this value only for legacy standalone NIM deployments with external URLs. It is unused when `K8S_NIM_DEPLOYMENT_KSERVE_MODE_ENABLED=true`. |

**Helm values.yaml:**
```yaml
backend:
  env:
    # Override if NIM deployments need more than 200Gi storage
    RESOURCES_STORAGE_MAX_SIZE: "200Gi"

    # Required only for legacy standalone NIM deployments with external URLs
    K8S_NIM_CLUSTER_HOST: "nim.example.com"
```

### 3.2 Providers

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `K8S_NIM_INGRESS_CLASS_NAME` added | **Review** | Sets the ingress class for legacy external NIM ingress. Default is `nginx`; set it only if your cluster uses a different ingress class. |

**Helm values.yaml:**
```yaml
backend:
  env:
    K8S_NIM_INGRESS_CLASS_NAME: "nginx"
```

---

## 4. Low-Priority / Optional Changes

### 4.1 Deployment & Scaling

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `K8S_KNATIVE_DEPLOYMENT_READY_GRACE_PERIOD_SEC` added | **No action (informational)** | Grace period after `Ready=False` before reporting `CRASHED`. Default is `15`. Helps prevent false CRASHED status during transient readiness probe failures. |

### 4.2 Providers

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `K8S_NIM_DEPLOYMENT_URL_SCHEMA` added | **Add** (optional) | Overrides the URL scheme used for resolved NIM service URLs. |
| 2 | `K8S_NIM_DEPLOYMENT_KSERVE_MODE_ENABLED` added | **Add** (optional) | Enables NIMService generation for KServe inference platform mode. Default is `false`, so existing legacy standalone NIM behavior remains unchanged. |

### 4.3 Configuration & API

| # | Change | Action | Details |
|---|--------|--------|---------|
| 1 | `spring.jpa.properties.org.hibernate.envers.store_data_at_delete` added | **No action (informational)** | Stores full deleted entity state for audit trail reconstruction. Default is `true`. |
| 2 | Validation properties renamed | **No action (informational)** | Validation resource properties were renamed from `app.resources.*` to `app.validation.resources.*`. Existing environment variable names remain unchanged. |

---

## Pre-flight Checklist

- [ ] Reviewed storage requirements for NIM deployments and adjusted `RESOURCES_STORAGE_MAX_SIZE` if needed.
- [ ] Removed custom `app.nim-service-expose-ingress-config.enabled` and `app.nim-service-expose-ingress-config.annotations` overrides.
- [ ] Kept `K8S_NIM_CLUSTER_HOST` only for legacy standalone NIM deployments with external URLs.
- [ ] Reviewed `K8S_NIM_INGRESS_CLASS_NAME` if the cluster does not use the `nginx` ingress class.
- [ ] Reviewed progress deadline fallback behavior for KServe and NIM deployments without startup probes.
